
//to set half square delay
void set_SquareDist(int);

//to set step size (takes int, so 1/ step size, i.e half step == 2)
void set_StepSize(int);


//Subroutine to move to center of a square and back
//send 'p' to pick up the piece, 'd' to drop
void ToCenter(char); 

//Standard move, includes centering and pick-up drop-off
void StandardMove(struct Point, struct Point);

//Move from first point to second - Starts && ends on bottom right
//corner so use ToCenter() before / after
void Move(struct Point, struct Point);

struct Point DeathMove(struct Point, struct Point, struct Point);

void MoveInit(int, int);

                     