#include <stdio.h>


typedef struct Point {
int X; 
int Y;
};


char CheckChar(char);

int CheckDeath(char*);
int CheckCastle(char*);

struct Point DecodeStartPos(char*);
struct Point DecodeEndPos(char*);
char* GetCastle(char*);